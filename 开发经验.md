# DSH 本地插件开发经验

## 最短流程

1. 插件单独维护：后端入口 `index.js`，前端入口 `client.js`，profile 初始配置放在 `cordis.patch.yml`。
2. 本地验证：`npm test`，然后 `npm pack` 生成 `.tgz`。
3. 安装到本地 profile：

   ```powershell
   pnpm add --save --lockfile=false --config.minimumReleaseAge=0 --offline "file:D:\AI\code\dsh-paste-to-path\dsh-paste-to-path-0.0.5.tgz"
   ```

   目标目录是 `%USERPROFILE%\.dsh\profiles\web`。安装后确认 `package.json` 和 `pnpm-lock.yaml` 都指向同一个 tarball。
4. 从 DSH 源码仓库根目录启动：`$env:PNPM_CONFIG_MINIMUM_RELEASE_AGE='0'; pnpm dsh web`。不要在 profile 目录直接执行 `pnpm dsh web`，那里通常没有 CLI。

## 接入标准设置卡

- 不改 DSH 核心源码。使用官方 slot：`ctx.slots.inject('settings.plugin.item', ...)`，再用 `ctx.slots.register` 注册插件卡片。
- 卡片结构照 Web UI 的 `PluginCard`：外层 `<li>`，标题区是可点击的 `<button aria-expanded>`，标题/描述/箭头在 header 中，详情 body 只在展开时渲染。
- CSS 使用 DSH 的 `--dsw-*` 变量、边框、圆角、间距和 hover/open 状态；不要再做一个常驻展开的白色自定义表单。
- 插件自己的设置不一定出现在 DSH 原生 `/api/settings.describe`。后端注册 `ctx.settings.register`，提供插件自己的 `settings/describe` 和 `settings/mutate`，前端用 `createBridgeScope` + `useSyncExternalStore` 镜像设置。
- 设置字段只在 profile patch 中给初始值；运行时从设置桥读取，不能把 `8000` 等默认值写死在前端。

## 这次踩坑

- 同一天发布的包会被 pnpm 的 `minimumReleaseAge` 拦截；本地安装使用 `--offline` 和 `--config.minimumReleaseAge=0`。
- 更新同名 tarball 可能被 pnpm 复用缓存；开发中递增版本号（本次为 `0.0.5`），再同步 lockfile。
- 固定 PNG/JPEG/WebP/GIF 勾选框不适合扩展需求；改成用户可输入、逗号或空格分隔的后缀列表，并统一去点号、转小写、去重。
- 用户配置在 `%USERPROFILE%\.dsh`，不应提交到插件仓库；`.tgz`、`node_modules` 等本地产物也应忽略。

## 验证标准

- `npm test` 全部通过。
- 实际请求 `/paste-to-path/settings/describe`，确认阈值和后缀列表来自 profile。
- 确认 DSH 核心源码没有改动，且 3080 服务重新加载了新 tarball。
